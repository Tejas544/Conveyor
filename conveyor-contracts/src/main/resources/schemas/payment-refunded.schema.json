{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://conveyor.internal/schemas/payment-refunded.schema.json",
  "title": "PaymentRefunded",
  "description": "ARCHITECTURE.md §6.1, §6.3. The envelope shape with eventType/payload constrained for PaymentRefunded.",
  "allOf": [
    { "$ref": "envelope.schema.json" },
    {
      "type": "object",
      "properties": {
        "eventType": { "const": "PaymentRefunded" },
        "schemaVersion": { "const": 1 },
        "payload": {
          "type": "object",
          "required": ["paymentId", "amount", "gatewayReference"],
          "properties": {
            "paymentId": { "type": "string", "format": "uuid" },
            "amount": { "type": "number", "exclusiveMinimum": 0 },
            "gatewayReference": { "type": "string", "minLength": 1 }
          },
          "additionalProperties": false
        }
      }
    }
  ]
}
